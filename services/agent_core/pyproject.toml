[project]
name = "agent_core"
version = "0.1.0"
dependencies = [
    "langchain[openai,google-vertexai,google-genai,anthropic,aws,ollama]",
    "lib_infisical"
]

[tool.uv.sources]
lib_infisical = { workspace = true }
