[project]
name = "lib_infisical"
version = "0.1.0"
dependencies = [
    "infisicalsdk",
]

[dependency-groups]
dev = [
    "ruff",
    "black",
    "mypy",
    "pytest",
    "pytest-cov",
    "pydantic",
    "pydantic-settings"
]

[tool.uv]
package = true